# 设备资产与流转管理系统 - 默认配置
# 运行：将本文件与 equipment.exe 放在同一目录即可生效；缺失时程序自动创建默认配置。

# 本地 Web 服务端口
port: 8080

# SQLite 数据库文件名（相对程序所在目录）
db_file: equipment.db

# 日志目录
log_dir: logs

# 备份保留份数（默认最近 30 份）
backup_keep: 30
