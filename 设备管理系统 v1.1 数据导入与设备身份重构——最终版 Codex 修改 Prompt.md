# 设备管理系统 v1.1：Excel 导入、设备身份、重复编号、无编号、历史借出与状态重构

你现在负责修改当前设备管理系统。

这是一次**数据模型 + Excel 导入 + 历史流转 + 前端展示 + 数据迁移 + 测试**的完整修复。

**不要只修一个导入 bug。必须从“真实设备身份”出发，重新保证整个系统的数据一致性。**

---

# 一、项目背景

当前系统是 Windows 本地设备资产/借出管理系统：

- Backend：Go + Gin + GORM
- Database：SQLite
- Frontend：React + TypeScript + Vite + Ant Design + ECharts
- Go embed 前端静态资源
- 本地单机运行
- 不允许引入：
  - PostgreSQL
  - MySQL
  - Redis
  - Docker
  - 云服务
  - 在线依赖
  - 服务端集群

当前已有：

- Dashboard
- Equipment Ledger
- Equipment Detail
- Borrowing
- Team Management
- Team Equipment
- Excel Import
- Backup / Restore
- Settings

---

# 二、必须遵守的核心原则

## 原则 1：equipment.id 才是真实设备身份

系统必须明确：

```text
equipment.id = 一台真实物理设备的唯一身份
```

绝对不能使用：

```text
equipment_no
name
model
name + model + equipment_no
```

作为设备唯一身份。

---

# 三、重新定义 F 列和 J 列职责

这是本次修改最重要的规则。

Excel：

```text
F = 台账设备编号
G = 时间
H = 公司
I = 台数
J = 借出设备编号
K = 备注
```

必须明确：

## F 列 = 设备资产全集

F 列负责决定：

> 系统中有哪些设备。

例如：

```text
F:
6060 6061 6062 6063
```

必须创建：

```text
equipment
6060
6061
6062
6063
```

---

## J 列 = 流转事件

J 列负责决定：

> 某些已经存在的设备发生了什么借出事件。

例如：

```text
F:
6060 6061 6062

J:
6061 6062
```

系统必须：

```text
创建设备：
6060
6061
6062

创建流转：
6061 → 某日期 → 某公司 → BORROW
6062 → 某日期 → 某公司 → BORROW
```

**绝对禁止：**

因为 J 列出现 `6061`，又额外创建一台 `6061`。

---

# 四、严禁使用 equipment_no 作为唯一键

当前存在：

```text
V002_equipment_unique_no.sql
```

其中：

```text
UNIQUE(name, model, equipment_no)
```

会导致同型号同编号的真实设备无法同时存在。

必须删除/废弃这一限制。

允许：

```text
设备 A:
equipment.id = 100
equipment_no = "6041"

设备 B:
equipment.id = 101
equipment_no = "6041"

设备 C:
equipment.id = 102
equipment_no = "6041"

设备 D:
equipment.id = 103
equipment_no = "6041"
```

这四台是四个不同设备。

---

# 五、设备编号必须原样保存

`equipment_no` 类型继续使用：

```text
TEXT / string
```

允许：

```text
6041
001
A01
JUKI-8700
车间A-01
缝制A-01
ABC-001
中文编号
```

禁止因为包含：

- 中文
- 字母
- 横杠
- 下划线
- 括号
- 其他符号

而直接拒绝设备。

---

# 六、不要把所有中文文本都当成设备编号

需要区分：

### 情况 A：明显设备编号

例如：

```text
车间A-01
缝制A-02
设备一号
ABC-001
```

可以作为：

```text
equipment_no
```

保存。

### 情况 B：明显是设备描述

例如：

```text
拉布机配件
拖布轮
```

这种不能强行当编号。

应该：

```text
equipment_no = NULL
remark = "拉布机配件"
```

并作为：

```text
未编号设备
```

处理。

### 情况 C：无法自动判断

不要静默丢弃。

必须在 Excel Import Preview 中显示：

```text
⚠ 需要人工确认
```

让用户决定：

```text
作为设备编号
```

或者：

```text
作为无编号设备 + 备注
```

---

# 七、重复编号处理

重复编号是正常业务数据，不是错误。

例如：

```text
6041
6041
6041
6041
```

必须创建 4 台设备：

```text
equipment.id   equipment_no   equipment_seq
1001           6041           1
1002           6041           2
1003           6041           3
1004           6041           4
```

前端显示：

```text
6041（1）
6041（2）
6041（3）
6041（4）
```

---

# 八、增加 equipment_seq

增加：

```sql
equipment_seq INTEGER NOT NULL DEFAULT 0
```

含义：

> 同一个设备编号下的展示序号。

注意：

```text
equipment_seq 不是设备身份
```

真实身份仍然是：

```text
equipment.id
```

---

# 九、display_no 不建议作为数据库身份字段

推荐后端返回：

```text
display_no
```

作为计算字段 / DTO 字段。

例如：

### 单个编号

```text
6041
```

### 重复编号

```text
6041（1）
6041（2）
6041（3）
6041（4）
```

### 无编号设备

如果：

```text
model = JUKI DDL-8700
```

则：

```text
JUKI DDL-8700（1）
JUKI DDL-8700（2）
JUKI DDL-8700（3）
```

如果没有型号：

```text
名称（1）
名称（2）
```

如果名称、型号都没有：

```text
未编号设备（1）
未编号设备（2）
```

---

# 十、无编号设备必须真正展开

Excel：

```text
设备名称：缝制熨斗
型号：T-3NS
台账数量：102
设备编号：无编号
```

不能只创建：

```text
1 条 equipment
```

必须创建：

```text
102 条 equipment
```

例如：

```text
T-3NS（1）
T-3NS（2）
...
T-3NS（102）
```

每条：

```text
equipment.id 不同
equipment_no = NULL
equipment_seq = 1..102
```

---

# 十一、无编号设备的序号分组规则

对于：

```text
equipment_no IS NULL
```

优先按照：

```text
model
```

分组。

如果 model 为空：

```text
name
```

如果 name + model 都为空：

```text
未编号设备
```

例如：

```text
JUKI DDL-8700（1）
JUKI DDL-8700（2）
JUKI DDL-8700（3）
```

不要显示：

```text
无编号
无编号
无编号
```

---

# 十二、数量校验

Excel：

```text
D = 台账数量
```

必须与展开后的设备数量一致。

例如：

```text
D = 4
F = 6041 6041 6041 6041
```

必须生成：

```text
4 台
```

如果：

```text
D = 10
F 只能解析出 8 台
```

不能静默导入。

必须：

```text
BLOCK
```

并在 Preview 显示：

```text
台账数量：10
解析设备：8
差异：2
```

---

# 十三、不要再因为重复编号 BLOCK

旧逻辑：

```text
同 name + model + equipment_no 重复
→ BLOCK
→ 整个设备组不导入
```

必须删除。

正确：

```text
重复编号
→ WARN
→ 正常展开
→ equipment_seq
→ 继续导入
```

---

# 十四、J 列借出事件解析

J 列不是简单字符串列表。

实际数据可能存在：

```text
11612（11369（带拖布轮）
```

或者：

```text
（2024 2027 4325（带拖布轮）
```

或者：

```text
（3313 2029 2001 1905(带拖布轮）
```

以及：

```text
492024（2026.9.4入南库）
```

因此必须建立：

```text
BorrowCellParser
```

不要直接：

```go
strings.Fields()
```

---

# 十五、J 列解析原则

J 列需要拆成：

```text
设备编号
备注/附加说明
```

例如：

```text
492024（2026.9.4入南库）
```

解析：

```text
equipment_no = "492024"
remark = "2026.9.4入南库"
```

---

对于：

```text
11612（11369（带拖布轮）
```

不要因为括号结构异常就直接 BLOCK。

应该：

1. 尝试提取明确编号
2. 保留原始文本
3. 对无法可靠解析的部分标记：

```text
needs_review = true
```

Preview 显示：

```text
原始借出编号：
11612（11369（带拖布轮）

解析结果：
11612
11369

备注：
带拖布轮

状态：
⚠ 请人工确认
```

---

# 十六、J 列无法可靠解析时绝对不能静默丢失

必须保存：

```text
raw_source_text
```

例如：

```text
11612（11369（带拖布轮）
```

即使自动解析失败，也必须：

```text
WARN / REVIEW
```

而不是：

```text
直接忽略
```

---

# 十七、历史借出必须进入系统

当前 Excel 中存在：

```text
G = 时间
H = 公司
I = 台数
J = 借出设备编号
```

这些数据不能再完全忽略。

必须创建历史流转记录。

例如：

```text
2015.4.12
莒县双发
6061 6062
```

产生：

```text
6061:
BORROW
borrow_date = 2015-04-12
borrower = 莒县双发

6062:
BORROW
borrow_date = 2015-04-12
borrower = 莒县双发
```

---

# 十八、同一设备可以有多次历史借出

例如：

```text
6061

2015-04-12 → 双发 → BORROW
2017-05-20 → 回库
2019-03-10 → 双发 → BORROW
2022-08-10 → 回库
2026-01-02 → 某公司 → BORROW
```

数据库必须支持：

```text
一个 equipment
    ↓
多个 flow_record
```

不能覆盖历史。

---

# 十九、不要因为历史借出记录就把设备永久设置为 BORROWED

这是非常重要的规则。

例如：

```text
2015 → BORROW
2016 → RETURN
```

最终状态：

```text
IN_STOCK
```

而不是：

```text
BORROWED
```

因此设备最终状态必须根据：

```text
该设备最后一条有效流转记录
```

推导。

---

# 二十、如何判断“当前仍然借出”

只有能够从 Excel 数据可靠判断：

```text
设备最后一次状态 = 借出
```

才能设置：

```text
equipment.status = BORROWED
```

如果只有：

```text
曾经借出
```

但没有可靠归还信息：

```text
不能假设当前仍然借出
```

应该：

```text
历史记录保留
当前状态保持原状态 / 待确认
```

绝对禁止凭空制造归还记录。

---

# 二十一、Excel 中的“入南库”等文字要作为归还线索

例如：

```text
492024（2026.9.4入南库）
```

其中：

```text
2026.9.4入南库
```

明显可能表示设备已经回库。

因此：

```text
borrow_record
```

应该能够表达：

```text
borrow_date
return_date
```

如果能够可靠解析：

```text
borrow_date = 原借出日期
return_date = 2026-09-04
```

如果无法可靠确定：

```text
raw remark 保留
needs_review = true
```

**不要猜日期。**

---

# 二十二、手工借出必须支持历史日期

当前：

```text
service/flow.go
```

使用：

```go
now := time.Now()
```

这是不够的。

修改 FlowRequest：

```go
BorrowDate *time.Time
```

或者：

```go
OccurredAt *time.Time
```

规则：

```text
用户没有填写
→ 默认今天

用户填写
→ 使用用户指定日期
```

并校验：

```text
不能晚于当前时间
```

---

# 二十三、手工归还同样保留真实时间

正常操作：

```text
今天归还
```

默认：

```text
time.Now()
```

但历史数据补录也允许：

```text
指定历史日期
```

---

# 二十四、借出记录必须保存快照

flow_record / borrow_record 至少保存：

```text
equipment_id
equipment_no_snapshot
equipment_name_snapshot
equipment_model_snapshot
borrower/team/company
borrow_date / occurred_at
return_date
remark
source
```

历史记录不能依赖当前设备名称才能正常显示。

---

# 二十五、导入来源必须可追踪

增加轻量级 Import Batch 能力。

建议：

```text
import_batch
```

至少：

```text
id
source_name
source_hash
created_at
status
total_rows
imported_count
warning_count
error_count
```

必要时增加：

```text
source_row_key
```

或者：

```text
source_hash + row_index
```

用于幂等。

---

# 二十六、重复导入必须幂等

同一个 Excel：

```text
第一次导入
```

创建设备。

再次导入相同文件：

```text
不能再次创建重复设备
不能再次创建重复借出历史
```

应该提示：

```text
该文件/批次已经导入
```

如果用户明确选择：

```text
重新导入
```

必须先明确：

```text
新增
覆盖
重新构建
```

不能默认复制。

---

# 二十七、注意现有数据库

当前数据库大约存在：

```text
equipment = 1477
flow_record = 1478
```

不要直接删除。

必须先：

```text
backup equipment.db
```

然后迁移。

---

# 二十八、数据库迁移 V003

新增：

```sql
equipment_seq INTEGER NOT NULL DEFAULT 0
```

删除：

```text
idx_equipment_name_model_no
```

保留：

```text
equipment_no 普通索引
```

可以：

```sql
CREATE INDEX idx_equipment_no
ON equipment(equipment_no);
```

---

# 二十九、迁移旧设备序号

对现有设备进行：

```text
equipment_no 分组
```

例如：

```text
6041
6041
6041
```

分配：

```text
1
2
3
```

无编号：

```text
model/name 分组
```

分配：

```text
1
2
3
```

注意：

> 迁移只能增加/修正展示序号，不能删除 equipment。

---

# 三十、迁移验收

迁移前记录：

```text
equipment_count
flow_record_count
borrow_record_count
```

迁移后必须：

```text
equipment_count 不减少
flow_record_count 不减少
borrow_record_count 不减少
```

如果发现变化：

```text
BLOCK
```

不要继续。

---

# 三十一、Import Preview 必须升级

导入前显示：

## 汇总

```text
Excel 台账数量：2147
预计新增设备：XXXX
已存在设备：XXXX
历史借出事件：XXXX
当前疑似借出：XXXX
无编号设备：XXXX
重复编号设备：XXXX
需要人工确认：XXXX
错误：XXXX
```

---

# 三十二、Preview 必须能够展示设备级结果

例如：

```text
设备编号   显示编号       名称       型号       状态
6041       6041（1）     ...        ...        正常
6041       6041（2）     ...        ...        正常
6041       6041（3）     ...        ...        正常
6041       6041（4）     ...        ...        正常
```

无编号：

```text
设备编号   显示编号
NULL       JUKI DDL-8700（1）
NULL       JUKI DDL-8700（2）
NULL       JUKI DDL-8700（3）
```

---

# 三十三、需要人工确认的内容必须单独列出

例如：

```text
⚠ J列无法可靠解析

原始值：
11612（11369（带拖布轮）

建议：
11612
11369

请确认。
```

用户确认后才允许最终导入。

---

# 三十四、Equipment 页面全面使用 display_no

所有前端页面：

- Equipment Ledger
- Equipment Detail
- Borrowing
- Team Equipment
- Dashboard
- Export
- Flow History
- Search Result
- Import Preview

统一使用：

```text
display_no
```

不要再各页面自己拼接。

---

# 三十五、React rowKey 必须使用 equipment.id

禁止：

```tsx
rowKey="equipment_no"
```

因为：

```text
6041
6041
6041
```

会冲突。

必须：

```tsx
rowKey="id"
```

---

# 三十六、搜索

支持：

```text
equipment_no
display_no
name
model
internal_code
```

例如搜索：

```text
6041
```

可以找到：

```text
6041（1）
6041（2）
6041（3）
6041（4）
```

---

# 三十七、Team Equipment 页面

必须继续保证：

```text
A班
 ├─ 缝纫机
 │   ├─ 6061
 │   ├─ 6062
 │   └─ 6063
 └─ ...
```

如果重复：

```text
6041（1）
6041（2）
```

如果无编号：

```text
JUKI DDL-8700（1）
JUKI DDL-8700（2）
```

---

# 三十八、Excel 导入不能破坏现有流转状态

如果数据库已经存在设备：

```text
equipment.id = X
```

再次导入时：

- 不得重新创建
- 不得覆盖已有历史
- 不得把 BORROWED 强制改成 IN_STOCK
- 不得删除团队关系
- 不得删除 borrower
- 不得删除 flow_record

必须通过稳定的来源身份进行 reconciliation。

---

# 三十九、特别处理“同编号不同设备”

例如：

```text
001
001
001
```

如果来自同一批设备数量展开：

```text
001（1）
001（2）
001（3）
```

但不同设备类别也可能出现：

```text
001
```

因此：

> `equipment_no` 不是全局唯一身份。

---

# 四十、导入匹配策略

对于历史数据库和新 Excel：

优先建立：

```text
source identity
```

不要简单使用：

```text
equipment_no
```

匹配。

推荐：

```text
source_file_hash
source_row_key
source_number_position
```

结合：

```text
name
model
equipment_no
equipment_seq
```

完成 reconciliation。

对于无编号设备：

```text
name
model
source row
sequence
```

用于导入匹配。

---

# 四十一、不要修改真实 Excel

Excel 只作为：

```text
读取源
```

不能：

```text
写回 Excel
```

不能自动修改原始文件。

---

# 四十二、原始数据必须保留

对于所有无法完全结构化的数据：

```text
raw_value
raw_source_text
remark
needs_review
```

必须尽量保留。

原则：

> 宁可 WARN + REVIEW，也不能静默丢数据。

---

# 四十三、导入必须使用事务

最终确认导入时：

```text
BEGIN TRANSACTION

设备创建
历史记录创建
状态更新
batch 创建
audit

COMMIT
```

任何关键错误：

```text
ROLLBACK
```

禁止出现：

```text
导入一半成功
导入一半失败
```

---

# 四十四、日志和审计

导入必须记录：

```text
谁
什么时候
什么文件
文件 hash
导入多少设备
多少历史记录
多少 warning
多少 error
```

---

# 四十五、测试必须覆盖以下场景

## Test 1：重复编号

输入：

```text
6041 6041 6041 6041
```

期待：

```text
4 equipment
equipment_no = 6041
equipment_seq = 1..4
display_no = 6041（1..4）
```

---

## Test 2：无编号

输入：

```text
JUKI DDL-8700
数量 = 3
无编号
```

期待：

```text
JUKI DDL-8700（1）
JUKI DDL-8700（2）
JUKI DDL-8700（3）
```

---

## Test 3：中文编号

输入：

```text
车间A-01
```

必须保留：

```text
equipment_no = 车间A-01
```

---

## Test 4：中文描述

输入：

```text
拉布机配件
```

必须：

```text
equipment_no = NULL
remark = 拉布机配件
```

---

## Test 5：J 列历史借出

输入：

```text
2024-01-01
公司A
6041
```

必须：

```text
创建 borrow/flow record
borrow_date = 2024-01-01
```

---

## Test 6：历史借出 + 归还

输入：

```text
2024-01-01 BORROW
2024-02-01 RETURN
```

最终：

```text
IN_STOCK
```

---

## Test 7：历史借出仍未归还

如果数据明确表明：

```text
2024-01-01 BORROW
```

之后没有归还：

```text
BORROWED
```

但只有在数据能够可靠支持这个结论时才能如此处理。

---

## Test 8：J 列复杂文本

输入：

```text
11612（11369（带拖布轮）
```

不能：

```text
丢弃
```

必须：

```text
WARN / REVIEW
```

---

## Test 9：重复导入

同一个 Excel 连续导入两次：

第一次：

```text
创建 N 台
```

第二次：

```text
新增 0 台
新增历史事件 0
```

---

## Test 10：数据库迁移

迁移前后：

```text
equipment_count 不减少
flow_record_count 不减少
```

---

# 四十六、真实 Excel 必须做最终验收

使用当前项目中的真实文件：

```text
设备借出总账(1).xlsx
```

不要只使用 mock 数据。

执行完整：

```text
parse
→ normalize
→ preview
→ validate
→ import
→ reconcile
→ verify
```

并输出：

```text
Excel 台账数量
解析设备数量
数据库设备数量
无编号数量
重复编号数量
历史借出数量
疑似当前借出数量
WARN 数量
REVIEW 数量
ERROR 数量
```

---

# 四十七、必须做数据对账

导入完成后生成 reconciliation report：

```text
Excel 中的设备
        ↓
数据库设备
```

检查：

```text
Excel 数量 ≠ DB 数量
编号缺失
编号多出
无编号数量异常
重复编号数量异常
历史借出设备无法匹配
```

任何异常必须明确列出。

---

# 四十八、旧逻辑必须清理

重点搜索并修改：

```text
parser.go
importer.go
service/flow.go
EquipmentPage.tsx
TeamEquipment
migrations/V002_equipment_unique_no.sql
```

以及所有：

```text
equipment_no unique
duplicate BLOCK
strings.Fields
time.Now()
rowKey=equipment_no
```

相关代码。

不要只修改一个文件。

---

# 四十九、修改前先进行代码审计

执行：

```text
git status
git log -5
```

然后搜索：

```text
equipment_no
equipment_seq
current_borrow_record_id
BorrowDate
OccurredAt
flow_record
borrow_record
V002
import
Excel
parser
display_no
```

确认实际代码后再修改。

---

# 五十、禁止盲目重构

只解决本需求相关问题。

不要：

- 重写整个项目
- 更换数据库
- 更换 ORM
- 重写前端框架
- 引入复杂 DDD
- 引入消息队列
- 引入 Redis
- 引入微服务
- 改变项目整体架构

保持当前项目轻量。

---

# 五十一、执行顺序

严格按照：

```text
Phase 0
代码和数据库备份
        ↓
Phase 1
数据库 V003
        ↓
Phase 2
Equipment Identity / equipment_seq
        ↓
Phase 3
Excel Parser
        ↓
Phase 4
Excel Importer
        ↓
Phase 5
Borrow / Flow 历史日期
        ↓
Phase 6
J列历史事件
        ↓
Phase 7
最终状态推导
        ↓
Phase 8
Frontend display_no
        ↓
Phase 9
Import Preview / Review
        ↓
Phase 10
真实 Excel 导入测试
        ↓
Phase 11
Reconciliation
        ↓
Phase 12
Regression Tests
        ↓
Phase 13
Documentation
```

---

# 五十二、每个 Phase 完成后必须验证

每阶段都执行：

```text
go test ./...
```

前端执行：

```text
npm test
```

或项目实际测试命令。

构建：

```text
go build ./...
```

前端：

```text
npm run build
```

不要等全部修改完成后才第一次测试。

---

# 五十三、完成后必须给出最终报告

最终报告必须包含：

## 1. 修改文件

```text
文件
修改内容
```

## 2. 数据库迁移

```text
V003
```

具体修改。

## 3. Excel 统计

```text
总数量
编号数量
无编号数量
重复编号数量
历史借出
Review
Warning
Error
```

## 4. 数据库最终统计

```text
equipment
flow_record
borrow_record
audit
```

## 5. 状态统计

```text
IN_STOCK
IN_TEAM
BORROWED
MAINTENANCE
SCRAPPED
...
```

## 6. 对账结果

必须明确：

```text
PASS
```

或者：

```text
FAIL
```

如果 FAIL：

```text
列出具体设备/行/原因
```

## 7. 测试结果

逐项：

```text
PASS / FAIL
```

## 8. Git

最后检查：

```text
git diff
git status
```

确认无无关修改。

---

# 五十四、最终验收标准

只有以下全部满足，才算完成：

- [ ] `equipment.id` 是唯一设备身份
- [ ] `equipment_no` 不再唯一
- [ ] 同编号设备可以存在多台
- [ ] `6041 × 4` 可以正确导入
- [ ] `equipment_seq` 正确
- [ ] `display_no` 正确
- [ ] 无编号设备全部导入
- [ ] 无编号设备按照数量展开
- [ ] 同型号无编号设备显示 `(1)(2)(3...)`
- [ ] 中文编号不会被错误丢弃
- [ ] 中文描述不会被强制当编号
- [ ] J 列不会额外创建设备
- [ ] J 列历史借出可以建立 flow/borrow record
- [ ] 同一设备可以拥有多次历史借出
- [ ] 历史日期可以保存
- [ ] 手工借出支持历史日期
- [ ] 当前状态不会因为历史记录被错误覆盖
- [ ] 能明确判断时才设置 BORROWED
- [ ] 无法判断时不会猜测
- [ ] 原始异常数据不会静默丢失
- [ ] J 列复杂数据进入 REVIEW
- [ ] Excel 重复导入不会重复创建设备
- [ ] 数据库迁移不丢设备
- [ ] 数据库迁移不丢历史
- [ ] 所有页面统一使用 display_no
- [ ] React rowKey 使用 id
- [ ] 搜索可以找到重复编号设备
- [ ] Team Equipment 正常
- [ ] Backup / Restore 正常
- [ ] 后端测试通过
- [ ] 前端构建通过
- [ ] Go 构建通过
- [ ] 真实 Excel 完成 reconciliation
- [ ] 无 ERROR
- [ ] 所有 REVIEW 项均明确列出

---

# 五十五、最重要的禁止事项

整个修改过程中，以下行为禁止：

```text
❌ 把 equipment_no 当唯一 ID
❌ 因为重复编号而跳过整个设备组
❌ 因为中文而拒绝设备
❌ 因为 J 列编号而新建设备
❌ 用 strings.Fields 粗暴解析 J 列
❌ 静默丢弃无法解析的数据
❌ 把所有历史借出设备都设为 BORROWED
❌ 凭空生成 RETURN
❌ 用 time.Now() 覆盖历史借出日期
❌ 重复导入产生重复设备
❌ 删除已有 flow_record
❌ 删除已有 equipment
❌ 为了修复问题重写整个项目
```

---

# 五十六、执行要求

现在开始执行。

但请严格遵循：

```text
先检查
→ 再修改
→ 每阶段测试
→ 最后真实 Excel 验收
```

如果发现业务数据存在无法自动判断的情况：

```text
不要猜
不要丢
不要静默处理
```

而是：

```text
保留原始值
+
WARN / REVIEW
+
最终报告列出
```

最终目标不是“Excel 能导入”。

真正目标是：

> **Excel 中每一台真实设备，在系统中都有且只有一个 equipment.id；每一台设备的编号、序号、历史流转和当前状态都能够被追踪；重复编号、无编号、中文编号以及历史借出数据都不会丢失。**